/*
package com.example.jni_mosysf;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;
import android.widget.TextView;

import com.example.jni_mosysf.databinding.ActivityMainBinding;

public class TestActivity extends AppCompatActivity {

    // Used to load the 'jni_mosysf' library on application startup.
    static {
        System.loadLibrary("jni_mosysf");
    }

    private ActivityMainBinding binding;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);

        binding = ActivityMainBinding.inflate(getLayoutInflater());
        setContentView(binding.getRoot());

        // Example of a call to a native method
        TextView tv = binding.sampleText;
        //tv.setText(stringFromJNI());
    }

    */
/**
     * A native method that is implemented by the 'jni_mosysf' native library,
     * which is packaged with this application.
     *//*

    //public native String stringFromJNI();
}*/
